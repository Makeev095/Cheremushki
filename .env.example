# Локально: npm run sheets:env  →  .env.local
# Vercel: READINGS_ADMIN_SECRET в Environment Variables

# Секрет для /admin/readings и /admin/readings-window
READINGS_ADMIN_SECRET=Makeev126

# На VPS: путь к данным (показания, настройки). Не менять после запуска.
# READINGS_DATA_DIR=/var/www/cheremushki/.data
